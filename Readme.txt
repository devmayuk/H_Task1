1. Configure the SQL port in python files.

2. Run the Flask_app, load all .py files

3. Run the localhost server via flask_app showing the index page.

4. If page not showing proper format, then check the directory of style.css should be with index.html

5. Make sure all the components mentioned in requirement.txt has been installed

6. Make sure all .py files are loaded before starting flask_app
